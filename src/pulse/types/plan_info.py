# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from ..core.unchecked_base_model import UncheckedBaseModel


class PlanInfo(UncheckedBaseModel):
    """
    Cumulative billing snapshot for the calling organization. Sourced from the `pulse-org-stats` aggregate table maintained asynchronously by the org-stats Lambda; the in-flight request's contribution is added on top so every response reflects post-request state. Returned by every endpoint that consumes credits (extract, schema, tables, split, form, and their batch / pipeline equivalents).
    """

    tier: typing.Optional[str] = pydantic.Field(default=None)
    """
    Billing tier, e.g. `"trial"`, `"growth"`, `"pulse_ultra_2"`.
    """

    total_credits_used: typing.Optional[float] = pydantic.Field(default=None)
    """
    Total credits consumed by the organization to date, including this request. The primary billing metric going forward.
    """

    pages_used: typing.Optional[int] = pydantic.Field(default=None)
    """
    Total pages processed by the organization to date, including this request. Kept for backward compatibility with clients that haven't migrated to `total_credits_used`.
    """

    note: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional human-readable note about billing state for this response (e.g. trial credits remaining). Omitted when no note applies.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
